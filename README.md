# Monthly-Expense-Split-Calculator-in-Python
This project is a simple ATM system built in Python to simulate basic banking operations. It includes a secure login system using a PIN, with a limit on incorrect attempts to prevent unauthorized access. Users can check their account balance, deposit money, withdraw funds (with sufficient balance validation), and exit safely
